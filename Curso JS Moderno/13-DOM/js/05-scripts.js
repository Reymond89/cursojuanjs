
// selecciona todos en este caso las cards

const cards = document.querySelectorAll('.card')
console.log(cards)

// si un elemento no existe !! se crea vacio

const noExiste = document.querySelectorAll('noExiste')
console.log(noExiste)